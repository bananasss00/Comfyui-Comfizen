# 📘 Полное руководство пользователя Comfizen

**Comfizen** — это мощная среда управления для ComfyUI, позволяющая превратить сложные JSON-воркфлоу в удобный, настраиваемый графический интерфейс с поддержкой пресетов, очередей, скриптов и сравнения результатов.

---

## 📋 Оглавление

1. [Начало работы и Интерфейс](#1-начало-работы-и-интерфейс)
2. [Управление Генерацией и Очередь](#2-управление-генерацией-и-очередь-queue)
3. [Система XY Grid (Сетки)](#3-система-xy-grid-сетки)
4. [Пресеты и Глобальное управление](#4-пресеты-и-глобальное-управление)
5. [Система Wildcards и Промптинг](#5-система-wildcards-и-промптинг)
6. [Галерея и Просмотр (Slider Compare)](#6-галерея-и-просмотр-slider-compare)
7. [Редактор Inpaint и Рисование](#7-редактор-inpaint-и-рисование)
8. [Дизайнер Интерфейсов (UI Constructor)](#8-дизайнер-интерфейсов-ui-constructor)
9. [Скриптинг (Python)](#9-скриптинг-python)
10. [Настройки и Горячие клавиши](#10-настройки-и-горячие-клавиши)

---

## 1. Начало работы и Интерфейс

### Основные понятия
Comfizen не заменяет ComfyUI, а работает как клиент.
* **Workflow (Воркфлоу)**: Файл `.json` в формате API (сохраненный в ComfyUI через "Save (API Format)"), к которому Comfizen добавляет слой интерфейса.
* **Сессия**: Comfizen автоматически запоминает состояние всех полей (текст, слайдеры, выбранные модели) во временный файл. При перезапуске программы вы продолжите с того же места.

### Вкладки Воркфлоу
* **Обычные вкладки**: Открытые файлы `.json` из папки `workflows`. Изменения в них сохраняются в сессию.
* **Виртуальные вкладки**: Если вы перетащите (Drag&Drop) сгенерированное изображение в окно программы, Comfizen откроет "виртуальную" вкладку с настройками, которые использовались для создания этого изображения.

### Управление окнами (Undocking)
Любую группу настроек (Group) или панель очереди можно "открепить" в отдельное плавающее окно.
* Нажмите кнопку ❐ в заголовке группы, чтобы вынести её в отдельное окно. Это удобно для многомониторных конфигураций.

---

## 2. Управление Генерацией и Очередь (Queue)

### Панель управления очередью
* **Кнопка Queue**: Отправляет текущие настройки в очередь на генерацию.
* **Бесконечная очередь (∞)**: Если включено, Comfizen будет автоматически добавлять новые задачи в очередь по мере выполнения старых.
* **Пауза/Возобновление**: Позволяет приостановить отправку задач на сервер (текущая задача на сервере завершится).
* **Stop/Interrupt**: Прерывает генерацию на сервере ComfyUI.
* **Clear Local**: Очищает локальную очередь ожидания в Comfizen (не затрагивает задачи, уже отправленные в ComfyUI).

### Менеджер Очереди (Queue Manager)
Нажмите кнопку списка рядом с кнопкой Queue, чтобы открыть детальный менеджер.
* **Просмотр изменений**: Для каждой задачи в очереди Comfizen показывает *только отличия* от исходного состояния (Diff). Вы увидите, какие именно параметры (seed, prompt, model) будут использованы.
* **Удаление**: Можно удалить конкретную задачу из списка ожидания до её отправки.
* **Поиск**: Фильтрация задач в очереди по имени воркфлоу или значениям параметров.

---

## 3. Система XY Grid (Сетки)

Встроенный инструмент для создания матриц сравнения (как в A1111), работающий поверх любого воркфлоу.
*Доступно через кнопку "XY" рядом с кнопкой Queue.*

### Настройка осей
Вы можете задать переменные для осей X и Y.
* **Источник (Field)**: Выберите любое поле из интерфейса (например, `CFG`, `Checkpoint`, `Denoise`).
* **Источник (Preset Group)**: В качестве оси можно выбрать целую группу пресетов.

### Ввод значений
* **Вручную**: Введите значения, каждое с новой строки.
  * *Пример для CFG*: `5`, `7`, `9`.
  * *Пример для Checkpoint*: `model_v1.safetensors`, `model_v2.safetensors`.
* **Поиск**: Используйте выпадающий поиск для добавления значений (моделей, семплеров или имен пресетов).

### Режимы и Опции
* **Create Grid Image**: Автоматически склеивает результаты в одну картинку-сетку.
* **Grid Mode**:
  * *Image*: Стандартный режим для картинок.
  * *Video*: Создает "сториборд" из кадров видео (полезно для AnimateDiff/VideoHelper).
* **Limit Cell Size**: Позволяет уменьшить разрешение ячеек в итоговой сетке, чтобы файл не получился огромным (настройка в мегапикселях).

---

## 4. Пресеты и Глобальное управление

Comfizen предлагает мощную систему пресетов для управления сложными настройками.

### Типы Пресетов
1. **Snippet (Фрагмент)**: Сохраняет значения конкретных полей внутри одной группы.
  * *Пример*: Пресет "High Quality" меняет Steps на 50 и Sampler на Euler.
2. **Layout (Макет)**: "Пресет пресетов". Он не хранит значения, а хранит список других сниппетов.
  * *Пример*: Layout "4k Realistic" активирует сниппет "Resolution 4k" и сниппет "Realistic Style".

### Управление слоями (Active Layers)
Comfizen использует систему слоев. Вы можете применить несколько пресетов одновременно.
* **Индикация**: Измененные пресетом поля временно подсвечиваются.
* **Конфликты**: Если новый пресет меняет то же поле, что и старый, старый слой автоматически отключается.
* **Статус**: Если вы изменили значение вручную после применения пресета, слой помечается как "Modified" (Изменен) оранжевым цветом. Вы можете "Переприменить" (Reapply) слой, чтобы сбросить ручные правки.

### Глобальные Пресеты (Global Presets)
Находятся в панели "Global Settings". Позволяют переключать состояние **всего воркфлоу** (нескольких групп одновременно) одним кликом.

---

## 5. Система Wildcards и Промптинг

### Синтаксис
* **Файлы**: `__filename__` — берет случайную строку из файла в папке `wildcards`.
  * Поддерживает вложенные папки: `__styles/anime__`.
  * Поддерживает Glob-паттерны: `__styles/*__` (выбор из всех файлов в папке).
* **Динамический выбор**: `{cat|dog|bird}` — случайный выбор одного варианта.
* **Веса (Weights)**: `(text:1.2)` — стандартный синтаксис ComfyUI.
  * *Горячие клавиши*: `Ctrl + Вверх/Вниз` на выделенном тексте изменяют вес.
* **Квантификаторы**:
  * `{2$$apple|banana|cherry}` — выбрать 2 уникальных варианта.
  * `{1-3$$apple|banana|cherry}` — выбрать от 1 до 3 вариантов.
  * `{2$$ and $$apple|banana|cherry}` — выбрать 2 варианта, разделив их словом " and ".
* **Вложенность**: Любая глубина вложенности: `{photo of a {cat|dog}|__artworks__}`.

### Wildcard Browser
Нажмите кнопку `...` рядом с полем промпта или используйте контекстное меню для открытия браузера вайлдкардов. Он позволяет искать файлы, просматривать их содержимое и вставлять в текст. Также умеет упаковывать/распаковывать вайлдкарды в YAML.

---

## 6. Галерея и Просмотр (Slider Compare)

### Галерея (Outputs)
* **Фильтрация**: По типу (Фото/Видео), Статусу (Сохранено/Не сохранено), Поисковый запрос.
* **Сортировка**:
  * *Newest/Oldest*: По дате.
  * *Similarity (Похожие)*: Группирует изображения по визуальной схожести (Perceptual Hash). Полезно для поиска дубликатов или вариаций одного сида.
* **Действия**: Выделите несколько изображений для массового сохранения или удаления (`Delete`).

### Fullscreen Viewer
* **Видео**: Встроенный плеер с поддержкой прокрутки (scrubbing) и зацикливания.
* **Метаданные**: Отображает разрешение, прогресс (индекс в серии).

### Slider Compare (Сравнение)
Выделите **два** изображения в галерее и нажмите кнопку **"Compare"** (или `Сравнить`).
* Откроется окно со слайдером "До/После".
* Работает как для изображений, так и для **видео** (синхронное воспроизведение).
* Поддерживает Drag&Drop файлов из проводника на левую или правую часть для сравнения с внешними референсами.

---

## 7. Редактор Inpaint и Рисование

Включается автоматически для полей типа `ImageInput` или `MaskInput`.

### Режимы
* **Mask**: Рисование ч/б маски для инпейнтинга (красный индикатор).
* **Sketch**: Рисование цветного скетча (для ControlNet Scribble/Canvas). Передача цвета в ComfyUI.

### Инструменты
* **Кисть**: Размер меняется слайдером или `Ctrl + Колесо мыши`.
* **Ластик**: Правая кнопка мыши.
* **Пипетка**: Инструмент взятия цвета с изображения (для режима Sketch).
* **Feather**: Размытие краев маски перед отправкой.
* **Вставка**: `Ctrl+V` вставляет картинку из буфера обмена прямо в редактор.

---

## 8. Дизайнер Интерфейсов (UI Constructor)

Инструмент для создания оболочки (`.json` воркфлоу для Comfizen).
**Вход**: Файл API JSON из ComfyUI.

### Разметка и Глобальные вкладки
В разделе **Layout** вы можете организовать структуру воркфлоу:

* **Глобальные вкладки (Tabs)**: Верхний уровень навигации, отображаемый под тулбаром. Идеально для разделения больших процессов (например, "Генерация", "Апскейл", "Настройки").
  * **Создание**: Нажмите кнопку `+` в правом верхнем углу панели Layout.
  * **Распределение**: Перетаскивайте (Drag-and-Drop) группы из списка "Unassigned" или других вкладок в нужную область.
  * **Нераспределенные**: Группы, не привязанные к вкладке, попадают в общий список, но для чистоты интерфейса лучше разнести их по вкладкам.

* **Группы (Groups)**: Сворачиваемые блоки настроек. Можно перемещать между глобальными вкладками.
* **Подвкладки (Sub-tabs)**: Внутри каждой группы можно создавать свои мини-вкладки (например, "Основные", "Дополнительно") для экономии места.

### Типы полей
1. **Any**: Универсальное поле (строка, число) с умной вставкой файлов.
  * **По умолчанию**: Вставка картинки (`Ctrl+V` или Drag&Drop) добавляет её Base64-содержимое. Вставка других файлов добавляет их пути.
  * **С `Shift`**: Чтобы вставить путь к картинке (а не её содержимое), используйте `Shift + Drag&Drop` или `Ctrl+Shift+V`.
2. **SliderInt / SliderFloat**: Слайдеры. Настраиваются Min/Max/Step/Precision.
3. **ComboBox**: Выпадающий список. Формат элементов: `Отображаемое::значение_api` или просто `значение`.
4. **Seed**: Поле с кнопкой блокировки/разблокировки и случайной генерации.
5. **Model**: Выпадающий список файлов. Требует выбора `Model Subtype` (checkpoints, loras и т.д.).
6. **Inpaint (Image/Mask)**: Графический редактор. Передает Base64.
7. **Node Bypass**: Чекбокс, который физически отключает/подключает ноды в графе (Bypass).
  * *Важно*: При использовании Bypass система делает снимок соединений, чтобы корректно восстановить их при включении ноды.
8. **Script Button**: Кнопка для запуска Python-скрипта (Action).
9. **Markdown**: Текстовый блок для инструкций или навигации.
  * **Цвета**: Используйте синтаксис `[Текст](color:red)` или `[Текст](color:#FF5500)` для выделения цветом.
  * **Ссылки на воркфлоу**: Используйте синтаксис `[Текст ссылки](wf://relative/path/to/workflow.json)` для создания кликабельных ссылок, открывающих другие воркфлоу внутри Comfizen.
  * **Стандартный Markdown**: Поддерживаются заголовки (`#`), списки (`-`), жирный (`**`) и блоки кода.
10. **Label / Separator**: Элементы оформления.

---

## 9. Скриптинг (Python)

Встроенный движок IronPython для автоматизации.

### Объект `ctx`
Доступен во всех скриптах:
* `ctx.prompt`: Словарь (JSON) текущего запроса к API. Можно читать и менять параметры.
  * *Пример*: `ctx.prompt['3']['inputs']['seed'] = 12345`
* `ctx.state`: Словарь для хранения данных между запусками (в рамках сессии).
* `ctx.settings`: Доступ к настройкам приложения.
* `ctx.log(msg)`: Вывод в консоль Comfizen.
* `ctx.queue(prompt)`: Поставить задачу в очередь.
* `ctx.output`: (Только в хуке `on_output_received`) Объект результата (картинка/путь).

### Хуки (Hooks)
Автоматические триггеры:
1. `on_workflow_load`: При открытии воркфлоу.
2. `on_queue_start`: При нажатии кнопки Queue.
3. `on_before_prompt_queue`: Перед отправкой каждой задачи (идеально для динамических изменений промпта).
4. `on_output_received`: Когда получена картинка.
5. `on_queue_finish`: Когда одна задача завершена.
6. `on_batch_finished`: Когда вся очередь пуста.

---

## 10. Настройки и Горячие клавиши

### Глобальные настройки
* **Адрес сервера**: IP и порт ComfyUI.
* **Пути**: Папки для сохранения результатов и сессий.
* **Формат сохранения**: PNG (с метаданными), JPEG, WebP. Опция удаления Base64 из файлов для экономии места.
* **Slider Defaults**: Правила для авто-определения диапазонов слайдеров в Дизайнере.
  * Формат: `NodeClass::FieldName=Min Max Step Precision`.

### Горячие клавиши
| Клавиши | Контекст | Действие |
| :--- | :--- | :--- |
| **Ctrl + Enter** | Везде | Запуск очереди (Queue) |
| **Ctrl + G** | Везде | Окно навигации по группам ("Перейти к...") |
| **Ctrl + V** | Поля / Inpaint | Вставка изображения или текста |
| **NumPad 4 / 6** | Fullscreen | Предыдущее / Следующее изображение |
| **NumPad 5** | Fullscreen | Сохранить изображение в папку Saved |
| **Esc** | Fullscreen | Выход |
| **Ctrl + Колесо** | Inpaint | Размер кисти |
| **Q** | Inpaint | Переключение Mask / Sketch |
| **Delete** | Галерея | Удалить выбранные файлы |
| **Ctrl + Up/Down**| Текст | Изменение веса токена `(text:1.1)` |