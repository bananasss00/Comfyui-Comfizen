<UserControl x:Class="Comfizen.TagEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Comfizen"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Input Area -->
        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <ComboBox x:Name="TagInput" IsEditable="True"
                      ItemsSource="{Binding AvailableTags, RelativeSource={RelativeSource AncestorType=UserControl}}"
                      KeyDown="TagInput_KeyDown"
                      local:TextBoxExtensions.PlaceholderText="Type or select a tag..."
                      Style="{StaticResource {x:Type ComboBox}}"/>
            
            <Button Grid.Column="1" Content="+" Command="{Binding AddTagCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                    CommandParameter="{Binding Text, ElementName=TagInput}"
                    Margin="5,0,0,0" Width="30"/>
        </Grid>

        <!-- Selected Tags Area -->
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Tags, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource TertiaryBackground}" CornerRadius="10" Margin="0,0,5,5" Padding="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" Foreground="{StaticResource TextBrush}" FontSize="11"/>
                            <Button Content="✕" 
                                    Style="{StaticResource TextButtonStyle}" 
                                    Foreground="{StaticResource TextSecondaryBrush}"
                                    Margin="5,0,0,0" 
                                    FontSize="10"
                                    VerticalAlignment="Center"
                                    Command="{Binding RemoveTagCommand, RelativeSource={RelativeSource AncestorType=local:TagEditor}}"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>