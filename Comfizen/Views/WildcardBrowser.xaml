<!-- WildcardBrowser.xaml.txt -->
<Window x:Class="Comfizen.Views.WildcardBrowser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:comfizen="clr-namespace:Comfizen"
        mc:Ignorable="d"
        Title="{comfizen:Translate WildcardBrowser_Title}" 
        Height="500" Width="350"
        Style="{StaticResource CustomWindowStyle}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid>
        <!-- The content from your XAML file is perfect and doesn't need changes. -->
        <Border Background="{StaticResource PrimaryBackground}" Padding="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="{comfizen:Translate WildcardBrowser_Insert}" IsDefault="True" Command="{Binding InsertCommand}" MinWidth="80"/>
                    <Button Content="{comfizen:Translate WildcardBrowser_Cancel}" IsCancel="True" Command="{Binding CancelCommand}" MinWidth="80" Margin="5,0,0,0"
                            Background="{StaticResource SecondaryBackground}" BorderBrush="{StaticResource TertiaryBackground}" BorderThickness="1"/>
                </StackPanel>
                
                <!-- NEW: Toolbar for utilities -->
                <Border DockPanel.Dock="Top" BorderBrush="{StaticResource TertiaryBackground}" BorderThickness="0,0,0,1" Margin="0,0,0,10" Padding="0,0,0,5">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="&#xE72C;" Style="{StaticResource IconButton}" Command="{Binding ReloadListCommand}" ToolTip="{comfizen:Translate WildcardBrowser_RefreshList}"/>
                        <Button Content="&#xE8DE;" Style="{StaticResource IconButton}" Command="{Binding ConvertWildcardsToYamlCommand}" Margin="10,0,0,0" ToolTip="{comfizen:Translate WildcardBrowser_PackToYaml}"/>
                        <Button Content="&#xE8B3;" Style="{StaticResource IconButton}" Command="{Binding ConvertYamlToWildcardsCommand}" Margin="5,0,0,0" ToolTip="{comfizen:Translate WildcardBrowser_UnpackFromYaml}"/>
                    </StackPanel>
                </Border>
                
                <TextBox x:Name="SearchTextBox" DockPanel.Dock="Top" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                
                <ListBox ItemsSource="{Binding FilteredWildcards}" SelectedItem="{Binding SelectedWildcard}"
                         Background="{StaticResource SecondaryBackground}" BorderThickness="1" BorderBrush="{StaticResource TertiaryBackground}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick" />
                            <Setter Property="Padding" Value="5,3"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource PrimaryAccentBrush}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource TertiaryBackground}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </DockPanel>
        </Border>
    </Grid>
</Window>